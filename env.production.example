# Production Environment Variables Template
# Copy this file to backend/.env.production and update with your actual values
# Based on your existing backend/.env configuration

# ================================
# FRONTEND ENVIRONMENT VARIABLES
# ================================

# Next.js Configuration (for root .env.production)
# NODE_ENV=production
# NEXT_PUBLIC_API_URL=http://YOUR_VPS_IP:5000
# For domain: NEXT_PUBLIC_API_URL=https://yourdomain.com

# ================================
# BACKEND ENVIRONMENT VARIABLES
# ================================

# Database Configuration (PRIMARY - matches your current setup)
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/alphacollect_db
# For your existing Render database, update the connection string:
# DATABASE_URL=postgresql://root:f4fbCzNyIAwHfdDonQvfQmlXLHnkyKNC@dpg-d3ai44qdbo4c738rug0g-a.oregon-postgres.render.com/alpha_database_qk9a

# JWT Secret (IMPORTANT: Change this for production!)
JWT_SECRET=your_very_secure_jwt_secret_key_here_minimum_32_characters_for_production

# Server Configuration
PORT=5000
NODE_ENV=production

# Email Configuration (SMTP settings - matches your current structure)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
EMAIL_USER=your-production-email@gmail.com
EMAIL_PASS=your-app-specific-password
EMAIL_FROM=AlphaWeb <no-reply@yourdomain.com>

# OTP Configuration (based on your current setup)
OTP_SKIP_EXPIRY=false
OTP_GRACE_MS=300000
# Remove OTP_MASTER in production for security

# ================================
# PRODUCTION-SPECIFIC SETTINGS
# ================================

# CORS Configuration
CORS_ORIGIN=http://YOUR_VPS_IP:3000
# For domain: CORS_ORIGIN=https://yourdomain.com

# SSL Configuration
USE_SSL=false
# Set to true when using HTTPS

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=50MB

# Logging Configuration
LOG_LEVEL=info
LOG_FILE_PATH=./logs/app.log

# ================================
# SECURITY NOTES
# ================================
# 1. Change JWT_SECRET to a strong, unique value
# 2. Use your own database (local PostgreSQL or cloud provider)
# 3. Configure production email SMTP settings
# 4. Remove or disable OTP_MASTER in production
# 5. Set appropriate CORS origins
# 6. Enable SSL when using HTTPS
