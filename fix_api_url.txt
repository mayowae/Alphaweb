Connected

============================================================
  STEP 1: Current alphaweb nginx config on VPS
============================================================
server {
    listen 80;
    server_name alphakolect.com www.alphakolect.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name alphakolect.com www.alphakolect.com;

    ssl_certificate /etc/nginx/ssl/alphakolect.com.crt;
    ssl_certificate_key /etc/nginx/ssl/alphakolect.com.key;

    client_max_body_size 50M;

    # Backend API: Only match /api, /health, /api-docs, /uploads
    location ~ ^/(api|health|api-docs|uploads)(/|$) {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    # Everything else goes to the Frontend (Next.js)
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

============================================================
  STEP 2: Update nginx alphaweb.conf — full backend routing + fix .env
============================================================
Written OK

============================================================
  STEP 3: Test and reload nginx
============================================================
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
nginx reloaded OK

============================================================
  STEP 4: Fix NEXT_PUBLIC_API_URL in .env on VPS
============================================================
Project dir not found
Could not find project dir

============================================================
  STEP 5: Update .env on VPS and rebuild Next.js
============================================================
Project .env not found on VPS — check manually

============================================================
  STEP 6: Current PM2 processes
============================================================
┌────┬──────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──────────┬──────────┐
│ id │ name                 │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │ cpu      │ mem      │ user     │ watching │
├────┼──────────────────────┼─────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┼──────────┼──────────┼──────────┼──────────┤
│ 0  │ alphaweb-backend     │ default     │ 1.0.0   │ cluster │ 122980   │ 33h    │ 1    │ online    │ 0%       │ 59.0mb   │ root     │ disabled │
│ 1  │ alphaweb-frontend    │ default     │ N/A     │ cluster │ 122989   │ 33h    │ 1    │ online    │ 0%       │ 27.1mb   │ root     │ disabled │
└────┴──────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

============================================================
  STEP 7: Rebuild Next.js with correct NEXT_PUBLIC_API_URL
============================================================
Next.js project not found — skipping rebuild. Update manually.

============================================================
  STEP 8: Restart Next.js frontend via PM2
============================================================
┌────┬──────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──────────┬──────────┐
│ id │ name                 │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │ cpu      │ mem      │ user     │ watching │
├────┼──────────────────────┼─────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┼──────────┼──────────┼──────────┼──────────┤
│ 0  │ alphaweb-backend     │ default     │ 1.0.0   │ cluster │ 122980   │ 33h    │ 1    │ online    │ 0%       │ 59.0mb   │ root     │ disabled │
│ 1  │ alphaweb-frontend    │ default     │ N/A     │ cluster │ 122989   │ 33h    │ 1    │ online    │ 0%       │ 27.3mb   │ root     │ disabled │
└────┴──────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

Use --update-env to update environment variables
Use --update-env to update environment variables
Use --update-env to update environment variables
Use --update-env to update environment variables
Use --update-env to update environment variables

=== PM2 status after restart ===
┌────┬──────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──────────┬──────────┐
│ id │ name                 │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │ cpu      │ mem      │ user     │ watching │
├────┼──────────────────────┼─────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┼──────────┼──────────┼──────────┼──────────┤
│ 0  │ alphaweb-backend     │ default     │ 1.0.0   │ cluster │ 122980   │ 33h    │ 1    │ online    │ 0%       │ 59.4mb   │ root     │ disabled │
│ 1  │ alphaweb-frontend    │ default     │ N/A     │ cluster │ 122989   │ 33h    │ 1    │ online    │ 0%       │ 27.3mb   │ root     │ disabled │
└────┴──────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

============================================================
  STEP 9: Quick verification — test alphakolect.com API endpoint
============================================================
=== Test /health endpoint ===
{"status":"OK","message":"Server is running"}
=== Test /merchant/login endpoint (should get JSON, not port error) ===
{"message":"Invalid credentials"}

Done.